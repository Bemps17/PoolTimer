# Relais WebSocket du miroir télécommande (Cloudflare Worker).
# Local : npm run mirror:dev  →  ws://localhost:8787
# Prod  : wss://h8timer-mirror.<compte>.workers.dev
VITE_MIRROR_WS_URL=ws://localhost:8787
